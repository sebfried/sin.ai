name: Build and Deploy

on:
  push:
    branches: [ master ]
    paths:
      - 'source/markdown/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: latest

    - name: Install dependencies
      run: npm install

    - name: Run build
      run: npm run build

    - name: Deploy to GitHub Pages
      uses: JamesIves/github-pages-deploy-action@4
      with:
        branch: master
        folder: docs
        target-folder: docs
        commit-message: 'JamesIves/github-pages-deploy-action@4'
        clean: true